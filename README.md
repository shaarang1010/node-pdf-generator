## PDF gen using pspdfkit

### Project setup

1. You will need to create a [PSPDFKit](https://pspdfkit.com/) account

2. Once you create an account, you will need to get an API_KEY to generate pdf.

3. Install dependencies using cmd `pnpm install` or `npm install`
